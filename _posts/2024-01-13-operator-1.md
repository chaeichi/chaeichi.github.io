---
layout: post
title: "Java의 정석 Chapter03. 연산자 (1) - 연산자"
subtitle: Java의 정석 3rd Edition | 남궁성
excerpt_image: /assets/images/excerpt-images/2024-01-13-operator-1.png
categories: 
    - Java
tags: 
    - Java
    - Java의 정석
    - 연산자
    - 피연산자
    - 연산자 우선순위
    - 산술변환
---
![banner](/assets/images/excerpt-images/2024-01-13-operator-1.png)

## 연산자(operator)
- `연산자(operator)` : 연산을 수행하는 기호(+, -, *, / 등)
- `피연산자(operand)` : 연산자가 연산을 수행하기 위한 대상(변수, 상수, 리터럴, 수식)
- 대부분의 연산자는 **두 개의 피연산자**를 필요로 함
- **하나** 또는 **세 개**의 피연산자를 필요로 하는 연산자도 존재
- **연산자는 피연산자로 연산을 수행하고 나면 항상 결과값을 반환함**

- `x + 3`이라는 식이 있을 때
    - `+`는 두 피연산자를 더해서 그 결과를 반환하는 `덧셈 연산자`
    - 변수 `x`와 상수 `3`은 `피연산자`
    - x는 5일 때, 결과값으로 8을 반환함

### 식(式)과 대입연산자
- `식(式, expression)` : 연산자와 피연산자를 조합하여 계산하고자 하는 바를 표현한 것
- 식을 계산하여 결과를 얻는 것을 식을 `평가(evaluation)`한다고 함
- 하나의 식을 평가(계산)하면, **단 하나의 결과를 얻는다.**
- 대입 연산자 `=`를 사용해서 변수와 같이 값이 저장할 수 있는 공간에 결과를 저장
    - y = 4 * x + 3;
- 식의 평가결과를 출력하기만 원하면 `println 메서드의 괄호()` 안에 직접 식을 써도 됨
    - System.out.println(4 * x + 3);

### 연산자의 종류
- 크게 산술, 비교, 논리, 대입 4가지로 나눌 수 있다.

![표3-1 연산자의 기능별 분류](/assets/images/javajungsuk3/3-1-table.png)

#### 피연산자의 개수에 의한 분류
- 피연산자의 개수가 `하나`면 `단항 연산자`
- 피연산자의 개수가 `두 개`면 `이항 연산자`
- 피연산자의 개수가 `세 개`면 `삼항 연산자`
- 대부분의 연산자는 이항 연산자
- 삼항 연산자는 오직 **? :** 하나뿐

- -3 - 5
    - 서로 다른 연산자의 기호가 같은 경우, 피연산자의 개수로 구분
    - 왼쪽의 것은 부호 연산자로 단항 연산자
        - 피연산자가 3 한 개
    - 오른쪽의 것은 뺄셈 연산자로 이항 연산자
        - 피연산자가 -3과 5 두 개

### 연산자의 우선순위와 결합규칙
#### 연산자의 우선순위
- 식에 사용된 연산자가 둘 이상인 경우, 연산자의 우선순위에 의해 연산순서가 결정됨

- **연산자 우선순위의 예와 설명**

|식|설명|
|-|-|
|-x + 3|단항 연산자가 이항 연산자보다 우선순위가 높다.<br/>그래서 x의 부호를 바꾼 다음 덧셈이 수행된다.<br/>여기서 '-'는 뺄셈 연산자가 아니라 부호 연산자다.|
|x + 3 * y|곱셈과 나눗셈이 덧셈과 뺄셈보다 우선순위가 높다.<br/>그래서 '3 * y'가 먼저 계산된다.|
|x + 3 > y - 2|비교 연산자 '>' 보다 산술 연산자 '+'와 '-'가 먼저 수행된다.<br/>그래서 'x + 3'과 'y - 2'가 먼저 계산된 다음에 '>'가 수행된다.|
|x > 3 && x < 5|논리 연산자 '&&'보다 비교 연산자가 먼저 수행된다.<br/>그래서 'x > 3'와 'x < 5'가 먼저 계산된 다음에 '&&'가 수행된다.<br/>식의 의미는 'x가 3보다 크고 5보다 작다'이다.|
|result = x + y * 3;|대입 연산자는 연산자 중에서 제일 우선순위가 낮다.<br/>그래서 우변의 최종 연산결과가 변수 result에 저장된다.|

- **주의해야 할 연산자 우선순위의 예와 설명**

|식|설명|
|-|-|
|x << 2 + 1|쉬프트 연산자(<<)는 덧셈 연산자보다 우선순위가 낮다.<br/>그래서 왼쪽의 식은 'x << (2 + 1)'과 같다.|
|data & 0xFF == 0|비트 연산자(&)는 비교 연산자(==)보다 우선순위가 낮으므로<br/>비교연산 후에 비트연산이 수행된다.<br/>그래서 왼쪽의 식은 'data & (0xFF == 0)'과 같다.|
|x < -1 \|\| x > 3 && x < 5|논리 연산자 중에서 AND를 의미하는 '&'와 '&&'가<br/>OR을 의미하는 '\|'와 '\|\|'보다 우선순위가 높다.<br/>이처럼 수식에 AND와 OR이 함께 사용되는 경우는<br/>다음과 같이 괄호를 사용해서 우선순위를 명확하게 하는 것이 좋다.<br/>'x < -1 \|\| (x > 3 && x < 5)'

- 우선순위가 확실하지 않다면, 먼저 계산되어야 하는 부분을 괄호로 묶어줌
- 괄호는 연산자의 우선순위를 임의로 지정할 때 사용하는 기호로 연산자가 아니다.

#### 연산자의 결합규칙
- 하나의 식에 같은 우선순위의 연산자들이 여러 개 있는 경우, `연산자의 결합규칙`에 따라 연산
- 대부분 `왼쪽에서 오른쪽`의 순서로 연산 수행
    - 3 + 4 - 5
    - (1) 3 + 4를 먼저 계산
    - (2) 3 + 4의 연산결과인 7과 5의 뺄셈을 수행
- `단항 연산자`와 `대입 연산자`만 `오른쪽에서 왼쪽`의 순서로 연산 수행
    - x = y = 3
    - y = 3과 x = 3을 하나의 문장으로 합쳐놓은 것
    - (1) y = 3이 먼저 수행
    - (2) x = 3이 수행되어 x에도 3이 저장
- 모든 연산자는 연산결과를 가짐
- 마찬가지로 대입 연산자는 우변의 값을 좌변에 저장하고, 저장된 값을 연산결과로 반환

#### 연산자의 우선순위와 결합규칙 정리
1. 산술 > 비교 > 논리 > 대입
    - 대입은 제일 마지막에 수행됨
2. 단항(1) > 이항(2) > 삼항(3)
    - 단항 연산자의 우선순위가 이항 연산자보다 높음
3. 단항 연산자와 대입 연산자를 제외한 모든 연산의 진행방향은 왼쪽에서 오른쪽

![표3-4 연산자의 우선순위와 결합규칙](/assets/images/javajungsuk3/3-4-table.png)

### 산술 변환(usual arithmetic conversion) 또는 일반 산술 변환
- **연산 전에 피연산자 타입의 일치를 위해 자동 형변환되는 것**
- 이항 연산자는 두 피연산자의 타입이 일치해야 연산 가능
- 대부분 두 피연산자의 타입 중 더 큰 타입쪽으로 일치
- 작은 타입에서 큰 타입으로 형변환 하는 경우, 자동적으로 형변환되므로 형변환 연산자 생략 가능
- 산술 변환은 `이항 연산`뿐만 아니라 `단항 연산`에서도 일어남
- 모든 연산에서 산술 변환이 일어나지만, **쉬프트 연산자(<<, >>)와 증감 연산자(++, --)는 예외**

#### 산술 변환의 규칙
1. 두 피연산자의 타입을 같게 일치(보다 큰 타입으로 일치)
    - long + int → long + long → long
    - float + int → float + float → float
    - double + float → double + double → double
2. 피연산자의 타입이 int보다 작은 타입이면 int로 변환
    - 피연산자 스택이 피연산자를 4byte 단위로 저장
    - 정수형의 기본 타입인 int(4byte)가 가장 효율적으로 처리할 수 있는 단위
    - byte + short → int + int → int
    - char + short → int + int → int

- 연산결과의 타입은 피연산자의 타입과 일치
- int와 int의 나눗셈 결과는 int로 실수형이 아니기 때문에 소수점 이하는 버려짐
    - 5 / 2 → 2
- 실수를 결과로 얻으려면 피연산자 중 어느 한 쪽을 실수형으로 형변환 해야함
    - int / (float)int → float / float → float
    - 5 / (float)2 → 5 / 2.0f → 5.0f / 2.0f → 2.5f